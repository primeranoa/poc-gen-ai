# Input

Deposita qui il file `ANATIT.csv` di BMED prima di eseguire lo script di conversione.

## File attesi

- `ANATIT.csv` — Anagrafica titoli BMED (165 colonne, separatore `;`, encoding UTF-8)

## Note

- Il file non viene versionato su Git (vedi `.gitignore`) perche troppo grande (>50MB)
- Recuperalo dall'area di scambio BMED o dalla cartella S3 del batch
