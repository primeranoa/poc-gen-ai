# Output

Qui viene generato il file `PFPANATIT.csv` dopo l'esecuzione dello script.

## File prodotti

- `PFPANATIT.csv` — Anagrafica titoli formato PROMPFT (177 colonne, separatore `;`)

## Note

- Il file non viene versionato su Git (vedi `.gitignore`) perche troppo grande
- Dopo la generazione, caricalo nella cartella `input/` dell'ambiente PROMPFT target
